module prime_filter [1.0]

import class Range from range;
import fn { iterator, next, is_consumed } from range;
import fn is_prime from primes;

if !is_prime(7919) {
    panic("Invalid prime calculation");
}

let r: Range = Range(0, 0, 10);

iterator(*<Range>r);
next(r);
is_consumed(r);
