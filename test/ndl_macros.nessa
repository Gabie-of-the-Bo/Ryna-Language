syntax ndl example from "#" s Arg(<ndl>, inner) s "#" {
    {$inner}
    {# \{ ", " }
        {$inner}
    {# \}}
}

class Ints {
    syntax from "[" # Arg(1{d}, ints) # "]";

    ints: Array<Int>;
}

let a: Ints = [1, 2, 3];