fn is_prime(n: &&Number) -> Bool {
    if n <= 1 {
        return false;
    }
    
    let i: Number = 1;

    while i < n - 1 {
        i = i + 1;

        if n % i == 0 {
            return false;
        }
    }

    return true;
}

let it: Number = 0;

while it < 50 {
    if is_prime(it) {
        print(it.deref<Number>());
    }

    it = it + 1;
}